export default function DashboardPage() {
  return (
    <div>
      <h1
        style={{ marginBottom: "1rem", fontSize: "1.75rem", fontWeight: 600 }}
      >
        Welcome to your Landlord Overview
      </h1>
      <p style={{ color: "var(--text-muted)" }}>
        This content area dynamically calculates structural positions matching
        fluid layout configurations safely.
      </p>
    </div>
  );
}
